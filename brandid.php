<?php
 echo 'hello world';
	/*
if($_POST) {

		$client = new SoapClient('http://www.ilovebrandsoutlet.com/index.php/api/soap/?wsdl');
		$session = $client->login('manometer','manometer1234');
		$customerInfo = array(
			/*'firstname' => 'test',
			'lastname' => 'test',
			'email' => $_POST['Field2'],
			'country' => $_POST['Field16'],
			'password_hash' => md5('password'),
			'store_id' => 5,
			'website_id' => 1
		);
		$newCustomerId = $client->call($session, 'customer.create',
		array($customerInfo));
		
		$defaultUsername = 'username'; // Used when first name isn't available

		$test = $client->call($session, 'customerurl.createUnique', array($newCustomerId, $defaultUsername));
		
        $test = $client->call($session, 'customerurl.addRewrites', $newCustomerId);
                
		$client->endSession($session);

	}	
	else{

		echo "<div style='width:500px;padding-top:50px;margin:auto;'>" .
			 "<p>Nothing was posted, or the post handshake was invalid.</p>" .
			 "</div>";
	}

*/
?>